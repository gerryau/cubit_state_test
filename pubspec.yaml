name: cubit_state_test
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to 
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  equatable: ^2.0.3
  bloc: ^8.0.3
  flutter_bloc: ^8.0.1
  freezed_annotation: ^1.1.0

dev_dependencies:

  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.8
  freezed: ^1.1.1

flutter:

  uses-material-design: true